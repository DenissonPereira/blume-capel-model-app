import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#ECEFF4',
        padding: 20
      },
      title: {
        fontSize: 22,
        fontWeight: 'bold',
      },
});